[phases.setup]
nixPkgs = ["python311", "gcc", "glib", "libGL", "zlib"]

[phases.install]
cmds = ["pip install --upgrade pip setuptools wheel", "pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --log-level info --preload"